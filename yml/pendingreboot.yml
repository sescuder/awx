---
- name: Check to see if the system needs a reboot
hosts: all
  assert:
    that:
      - ansible_reboot_pending == false
    fail_msg: "{{ ansible_hostname }} needs a reboot"
    success_msg: "{{ ansible_hostname }} does not need a reboot"
